name: Fetch subtitles (Bilibili / YouTube)

on:
  workflow_dispatch:
    inputs:
      url:
        description: "B站或YouTube视频链接"
        required: true
        type: string
      langs:
        description: "字幕语言(逗号分隔/可正则)，示例：zh-Hans,zh-Hant,en.*"
        required: false
        default: "zh-Hans,zh-Hant,en.*"
        type: string
      user_agent:
        description: "可选：浏览器UA（遇到风控可配合cookies），留空则不传"
        required: false
        default: ""
        type: string
      proxy:
        description: "可选：代理，例如 http://user:pass@host:8080 或 socks5://host:1080"
        required: false
        default: ""
        type: string

jobs:
  subs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install yt-dlp
        run: python -m pip install -U yt-dlp

      - name: Write cookies.txt (optional)
        env:
          YTDLP_COOKIES_TXT: ${{ secrets.YTDLP_COOKIES_TXT }}
        shell: bash
        run: |
          printf '%s' "$YTDLP_COOKIES_TXT" | tr -d '\r' > cookies.txt

      - name: Download subtitles only
        env:
          URL: ${{ inputs.url }}
          LANGS: ${{ inputs.langs }}
          UA: ${{ inputs.user_agent }}
          PROXY: ${{ inputs.proxy }}
          HAS_COOKIES: ${{ secrets.YTDLP_COOKIES_TXT != '' }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p subs

          EXTRA_ARGS=()
          if [ "$HAS_COOKIES" = "true" ]; then
            EXTRA_ARGS+=(--cookies cookies.txt)
          fi
          if [ -n "$UA" ]; then
            EXTRA_ARGS+=(--user-agent "$UA")
          fi
          if [ -n "$PROXY" ]; then
            EXTRA_ARGS+=(--proxy "$PROXY")
          fi

          yt-dlp \
            --js-runtimes node \
            --skip-download \
            --write-subs \
            --write-auto-subs \
            --sub-langs "$LANGS" \
            --sub-format "vtt/best" \
            --convert-subs "srt" \
            -o "subs/%(title).200s_%(id)s.%(ext)s" \
            "${EXTRA_ARGS[@]}" \
            "$URL"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: subtitles
          path: subs/
